<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vehiculos">

    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".InsertarCoche"
            android:configChanges="orientation|screenSize"/>
        <activity android:name=".InsertarGastos"
            android:configChanges="orientation|screenSize"/>
        <activity android:name=".ModificarCoche"
            android:configChanges="orientation|screenSize"/>
        <activity android:name=".ModificarGastos"
            android:configChanges="orientation|screenSize"/>
        <activity android:name=".BorrarCoche"
            android:configChanges="orientation|screenSize"/>
        <activity android:name=".BorrarGastos"
            android:configChanges="orientation|screenSize"/>
        <activity android:name=".MainActivity"
            android:configChanges="orientation|screenSize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>